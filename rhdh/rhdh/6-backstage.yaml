---
apiVersion: rhdh.redhat.com/v1alpha3
kind: Backstage
metadata:
  name: developer-hub
  namespace: demo-project
  labels:
    app.kubernetes.io/name: backstage
spec:
  application:
    appConfig:
      configMaps:
        - name: app-config-rhdh
      mountPath: /opt/app-root/src
    extraEnvs:
      envs:
        - name: NODE_TLS_REJECT_UNAUTHORIZED
          value: "0"
      secrets:
        - name: rhdh-secrets            
    replicas: 1
    route:
      enabled: true
      host: rhdh.apps.cluster-nblnq.dynamic.redhatworkshops.io
      subdomain: apps.cluster-nblnq.dynamic.redhatworkshops.io
  database:
    enableLocalDb: true
...
